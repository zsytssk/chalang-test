
alpha = { 'a'..'z' | 'A'..'Z' }
digit = { '0'..'9' }
dash = {"_"}
char = { alpha | "digit" | dash}
name= {alpha~char*}      // for instance, "1362 + 12"
WHITESPACE = _{ " " | "\t" }
END_LINE = _{ ";" | "\n" }
Define = {"let" | "const"}
// statement = {Define ~ name ~ "=" ~ NUMBER+ ~ END_LINE}
statement = {Define ~ name ~ "=" ~ name ~ END_LINE}